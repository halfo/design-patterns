subprojects {
    apply plugin: 'java'
        sourceCompatibility = 1.7
        version = '1.0'

        repositories {
            mavenCentral()
        }

    jar {
        manifest {
            attributes 'Main-Class': 'io.github.halfo.Main'
        }
    }
}
